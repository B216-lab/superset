# All these todos should become specs in the `specs/` directory for futher work on

- [ ] Prepare SSO support configuration
- [ ] Connect database to Superset, so admin don't need to do it manually in the Superset UI after the service is up for the first time
- [ ] Suggest compose.yml file that would run in production server with sensitive data like ports, passwords, domains, usernames, database connection details and so on passed as variables like to Jinja template in Ansible project for example
- [ ] Add a uv setup for the dependencies mentioned in next todo
- [ ] Add pre-commit configuration to check yaml lint, hadnolint, shellcheck, ruff, ty
